---
const pathname = Astro.url.pathname;
// components
import Link from "../elements/Link.astro"

// content
const nav = [
    { title: "/home", href: "/salahdabbech" },
    { title: "/projects", href: "/salahdabbech/projects" },
    { title: "/about", href: "/salahdabbech/about" },
    { title: "/writing", href: "/salahdabbech/blog" },
    { title: "/contact", href: "/salahdabbech/contact" },
]
---

<nav class="bg-base-900 flex flex-row flex-wrap sticky top-0">
    {
        nav.map((navLink) => {
            const isActive = navLink.href === "/" ? pathname === "/" : pathname.startsWith(navLink.href);
        return (
            <Link
                variant={'nav'} 
                href={navLink.href}
                text={navLink.title}
                aria-current={isActive ? "page" : undefined}
                class={isActive ? "bg-base-950 border-t-2 border-base-900" : "" }
            />
        )})
    }
</nav>
